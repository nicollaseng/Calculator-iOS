import React, { Component } from 'react';
import {
  StyleSheet,
  Text,
  View
} from 'react-native';

export default props => (
   <View>
       <Text>Visor</Text>
    </View>
)

const styles = StyleSheet.create({
  
});
