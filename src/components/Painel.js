import React, { Component } from 'react';
import {
  StyleSheet,
  Text
} from 'react-native';

const Painel = props => (
    <Text>Painel</Text>
)

const styles = StyleSheet.create({
  
});

export {Painel}